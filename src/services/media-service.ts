export async function uploadVideo(videoPath: string, mimeType: string): Promise<string> {
  try {
    console.log('Tentativo di caricamento video...');
    // Implementazione del servizio di upload video
    return 'Video caricato con successo';
  } catch (error) {
    console.error('Errore durante il caricamento del video:', error);
    throw error;
  }
}
export async function uploadVideo(videoPath: string, mimeType: string): Promise<string> {
  try {
    console.log('Tentativo di caricamento video...');
    // Implementazione del servizio di upload video
    return 'Video caricato con successo';
  } catch (error) {
    console.error('Errore durante il caricamento del video:', error);
    throw error;
  }
}
